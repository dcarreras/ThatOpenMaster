# ThatOpenMaster

Course exercises for building static and interactive websites.

- `01-Creating Static Websites` — HTML/CSS project dashboard (open `index.html`).
- `02-Creating Interactive Websites` — Vite + TypeScript version.

To run the interactive module:
```sh
cd "02-Creating Interactive Websites"
npm install
npm run dev
```
